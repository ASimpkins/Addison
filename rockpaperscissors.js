
//set this to whatever choice youd like
var playerChoice = "r";
// computer's random choice

var choices = ['r','p','s'];
var compChoice = choices[Math.floor (Math.random()*3)];
//set them equal remeber us to show they are = and
// use () around the strings and statement console.log printsssssss
If (playerChoice=='r' && compChoice='p'); {
    console.log("player wins")
}
// && tells the computer to figure out what happens
If (playerChoice&&compChoice); {
}
Math.random(3);


